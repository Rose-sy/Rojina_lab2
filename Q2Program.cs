// Program.cs
using System;
using Namespace1;  // Importing the Namespace1

namespace Namespace2
{
    class Program
    {
       /* static void Main(string[] args)
        {
            // Creating an object of Employee from Namespace1
            Employee emp = new Employee("Rojina Adhikari", "rojinaaa300@gmail.com", 70000);

            // Accessing method of the Employee class
            emp.DisplayEmployeeDetails();

            // Accessing fields of the Employee class
            Console.WriteLine($"\nAccessing Fields Directly:");
            Console.WriteLine($"Name: {emp.Name}");
            Console.WriteLine($"Email: {emp.Email}");
            Console.WriteLine($"Salary: {emp.Salary:C}");

            // Keep the console window open
            Console.ReadLine();
        }*/
    }
}
